{#- tool_spec for LightRAG RAG / naive RAG final answer -#}
{
  "type": "function",
  "function": {
    "name": "generate_rag_answer",
    "description": "Tool-only: call once; no assistant text outside tool_calls. Generate the final RAG answer from context and the retrieval query (English, for the reasoning stage).",
    "parameters": {
      "type": "object",
      "properties": {
        "answer": {
          "type": "string",
          "minLength": 1,
          "description": "Final answer text in English for the reasoning stage (not user-facing egress)."
        }
      },
      "required": ["answer"],
      "additionalProperties": false
    }
  }
}
