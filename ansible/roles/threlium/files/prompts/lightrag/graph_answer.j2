{#- LLM-facing prose sample of LightRAG retrieval for <graph-answer> (enrich → reasoning).
    Kwargs from GraphAnswerView.for_graph_answer_jinja(); not the internal kg_query_context overlay.
-#}
{% if entities or relations %}
```mermaid
flowchart LR
{% for e in entities %}
    {{ e.id }}["{{ e.mermaid_label }}"]
{% endfor %}
{% for r in relations %}
{% if r.mermaid_edge_label %}
    {{ r.src_node }} -->|{{ r.mermaid_edge_label }}| {{ r.tgt_node }}
{% else %}
    {{ r.src_node }} --> {{ r.tgt_node }}
{% endif %}
{% endfor %}
```
{% endif %}

{% if answer %}
## Answer
{{ answer }}
{% endif %}
