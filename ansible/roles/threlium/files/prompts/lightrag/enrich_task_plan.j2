{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    enrich task-plan seed: decompose the user's request into a small ordered set of
    concrete subtasks for the durable task ledger (anti-drift anchor).
    Variables: incoming_user_message (str); existing_subtasks (list of {content_id, text, status}). -#}
You are planning the work for an email-based reasoning agent. Break the user's request into a SMALL set (1–6) of concrete, verifiable subtasks that together fully satisfy it. Each subtask is one action or outcome, phrased so completion is checkable.

Where it fits the request, cover the phases the work needs (not every request needs all three):
- discover — find a fact, a relation, or where something lives in the project/graph;
- verify — confirm a relation, an invariant, or a multi-step conclusion before asserting it;
- deliver — produce the answer to the user or the artifact.
Express each subtask's phase through its VERB, never a literal tag: discover → Find/Locate/Identify/Survey; verify → Confirm/Verify/Check/Prove; deliver → Draft/Write/Answer/Compose.

## Output contract
- Output ONLY a JSON array of strings (the subtask texts), nothing else (no preamble, no markdown fences, no role-play).
- Example: ["Find where X is handled", "Confirm X relates to Y per the graph", "Draft the reply section on X"]. If the request is trivial, return a single deliver subtask describing the whole answer, e.g. ["Answer the user's question about X"].

`incoming_user_message` is post-distill canonical intent; do not re-import quoted prior thread from email forwards.

{% if existing_subtasks %}
A ledger already exists for this thread. Do NOT duplicate these — if a subtask is still valid, reuse its EXACT text verbatim (identity is content-addressed). Only add genuinely new subtasks; return [] if nothing new is needed:
{% for s in existing_subtasks %}
- [{{ s.status }}] {{ s.text }}
{%- endfor %}
{% endif %}

If the user request lists explicit action bullets, include matching discover/verify/deliver subtasks; do not duplicate the same work twice.

Canonical intent (English, ingress distill):
{{ incoming_user_message }}
