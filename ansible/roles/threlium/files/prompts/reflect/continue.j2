{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    reflect continue cycle user-visible body. -#}
[Threlium reflect — continue]

Subject: {{ subject or "(no subject)" }}

You requested another reasoning pass. Below is your previous reasoning output
verbatim. The next message you will see in the reasoning stage is the result of
this body being routed through ingress and enrich, so the unified knowledge/memory
graph will be re-traversed (a fresh hybrid retrieval) and merged in. Naming the
entities and links you want expanded makes that re-traversal richer.

Guidelines for the next reasoning step:
- Treat the text below as your own previous draft, not as a user message.
- Use the freshly merged LightRAG context to refine, extend, or correct it.
- Do NOT copy the previous draft verbatim into your next decision body — keep
  only what is still relevant and add what LightRAG newly contributes.
- If you are now confident, produce the response via response_finalize (or
  response_append → response_finalize for long answers); otherwise call reflect
  again.

Strategy for this pass:
- Focus on what was missing in the previous draft. Ask: what gap did clarification_request target?
- If fresh context adds new facts — integrate them.
- If fresh context adds nothing — do NOT reflect again. Finalize and note any remaining gaps.
- Compare the entities and links you named in the previous reflect's clarification_request against the freshly merged <knowledge_graph>: if not one of those hypotheses was confirmed or refuted, this corridor is a dead end — do not reflect again. Instead run a targeted memory_query anchored on a concrete entity name, or finalize stating the gap explicitly.
- reflect is a REMAP (rebuild the context), not a proof: route a logical/consistency/invariant claim to formal_reason on the next hop, not to another reflect.
- Reformulate, do not regurgitate. Do not reintroduce content already established in the previous draft unless fresh context corrects it.
- If the previous pass skipped discovery (no memory_query/cli for an implementation task), run memory_query or cli_intent inventory this pass before proposing new code.
- Target the unresolved root cause, not the symptom: if the previous draft glossed over WHY something holds (or an assumption it never verified), dig into that this pass. Route by the kind of gap: a logical/consistency/invariant claim → formal_reason to confirm it; a missing fact → memory_query (reflect is for a broad context rebuild, not for proving an inference).
- If you discovered a problem in the material (a contradiction, gap, or error), pursue and resolve it this cycle rather than carrying it forward unaddressed.

--- previous reasoning output ---
{{ previous_reasoning }}
--- end previous reasoning output ---
