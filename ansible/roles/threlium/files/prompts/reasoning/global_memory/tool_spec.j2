{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    docs/MEMORY_TABLE.md §2: tool-spec for route `global_memory`. -#}
{
  "type": "function",
  "function": {
    "name": "global_memory",
    "description": "Tool-only: call once; no assistant text outside tool_calls. Maze move — ANCHOR a landmark for all future expeditions. Store a global fact about the agent, tasks, policies, or stable environment. The note is written into the unified knowledge/memory graph — the SAME graph as domain documentation and past conversations — as entities/relations retrievable across all threads via memory_query (and surfaced in <global_memory>). Use ONLY for facts relevant to future threads, not a single dialog turn (use thread_memory for thread-local notes). A good trigger is persisting a durable correction or relationship you discovered while investigating, so future threads retrieve the corrected fact. After discovery: stable architectural patterns that prevent duplication across threads (e.g. 'All channel bridges must follow the shared bridge contract in bridges/; study matrix.py and email.py together'). When NOT to use: do not persist transient reasoning scratch, and do not store conversation-local context here — that is thread_memory. Boundary: global_memory = stable, reusable across all threads; thread_memory = scoped to this conversation.",
    "parameters": {
      "type": "object",
      "properties": {
        "reasoning": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500,
          "description": "Brief explanation of why this fact is globally relevant across all threads."
        },
        "note": {
          "type": "string",
          "minLength": 1,
          "maxLength": 8000,
          "description": "Global note text (UTF-8). Third person, self-contained; read during global-scope LightRAG search."
        }
      },
      "required": ["reasoning", "note"],
      "additionalProperties": false
    }
  }
}
