{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    docs/ARCHITECTURE.md §6.1: tool-spec for route `egress_router`. -#}
{
  "type": "function",
  "function": {
    "name": "egress_router",
    "description": "Tool-only: call once; no assistant text outside tool_calls. Maze move — WRONG EXIT (legacy): do not take this door; use response_finalize instead. Legacy/inactive route — the reasoning stage MUST NOT call this directly; deliver every user reply via response_finalize, which routes to egress internally. Sends final reply on the original channel (email / Matrix / Telegram) when the answer is complete and no further CLI or reasoning cycle is needed.",
    "parameters": {
      "type": "object",
      "properties": {
        "subject": {
          "type": "string",
          "minLength": 1,
          "description": "Reply subject (short, no line breaks)."
        },
        "body": {
          "type": "string",
          "minLength": 1,
          "description": "Reply body for the user (UTF-8). For full JSON or large context, pass content verbatim without placeholders."
        }
      },
      "required": ["subject", "body"],
      "additionalProperties": false
    }
  }
}
